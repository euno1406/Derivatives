1. 파이썬 프로그램 설치 https://www.python.org/downloads/
2. 패키지 설치 cmd 에서 pip install streamlit numpy plotly
3. 실행 python -m streamlit run d:\els_dashboard.py
python -m streamlit run d:\fx_forward_sim.py
pip install streamlit numpy plotly
